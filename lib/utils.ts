/**
 * Re-export utilities at the canonical path the website-factory
 * component kit expects (`@/lib/utils`). Thin barrel file — the
 * actual implementations live under `lib/utils/`.
 */
export { cn } from "./utils/cn";
